[access]
	inheritFrom = ta
[access "refs/*"]
	owner = group ldap/akraino-gerrit-ta-openstack-ansible-haproxy_server-committers
[project]
	description = This projects is re-used from the upstream Openstack-Ansible project (https://github.com/openstack/openstack-ansible ) for the purpose of deploying HA-proxy. These services are used by various middleware, and deployer components.
